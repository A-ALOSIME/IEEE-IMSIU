import type {APIRoute} from 'astro';import {publicState} from '../lib/public-data';export const GET:APIRoute=async({url})=>{const state=await publicState(),paths=['/','/events','/events/archive','/join','/articles','/impact','/about','/collaboration','/privacy','/en/',...state.events.filter(x=>x.status==='published').map(x=>`/events/${x.id}`),...state.articles.filter(x=>x.status==='published').map(x=>`/articles/${x.slug}`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(path=>`<url><loc>${url.origin}${path}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml'}})};
